export default {}
// import type { VariantProps } from 'cva'
// import { cva } from 'cva'
// import type { ClassProp } from 'cva/dist/types'
// import { ensure } from '../utils/ts-helpers'

// const button = cva(['base styles'], { variants: { theme: { jsl: 'bg-accent' } } })

// type ButtonProps = VariantProps<typeof button> & ClassProp

// export const Button = ({ theme,  }: ButtonProps) => {
//   return <div class={button({ theme })} />
// }

// export const Usage = () => <Button tx={[tx.blur]} />

// const tx = ensure<Record<string, {in: string, out: string, base?: string}>>()({
//   blur: {
//     in: '',
//     out: ''
//   }
// })
